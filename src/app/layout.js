import './globals.css'

export const metadata = {
  title: 'Eksempel om states',
  description: 'Generated by create next app',
}

export default function RootLayout({ children }) {
  return (
    <html lang="da">
      <body>{children}</body>
    </html>
  )
}
